# =============================================================================
# Local Development Environment Configuration Template
# =============================================================================
# This is a template for local development.
# Copy this file to .env.local and customize as needed.
# The .env.local file will NOT be committed to git.

# =============================================================================
# Environment Selection
# =============================================================================
DJANGO_ENVIRONMENT=local

# =============================================================================
# Django Core Settings
# =============================================================================
DEBUG=True
SECRET_KEY=local-dev-key-generate-new-one-for-your-machine
ALLOWED_HOSTS=localhost,127.0.0.1

# =============================================================================
# Database
# =============================================================================
# Local development uses SQLite by default (no DATABASE_URL needed)
# SQLite database will be created automatically at db.sqlite3
# Uncomment below to use PostgreSQL locally instead:
# DATABASE_URL=postgresql://user:password@localhost:5432/pss_local

# =============================================================================
# PII Encryption (Development Only)
# =============================================================================
# This is a development-only key - NOT FOR PRODUCTION
FIELD_ENCRYPTION_KEY=dev-only-key-replace-in-production-32bytes!

# =============================================================================
# Frontend Configuration
# =============================================================================
FRONTEND_URL=http://localhost:5173
# CORS is automatically configured for localhost in local.py

# =============================================================================
# Email Configuration
# =============================================================================
# Local development uses console backend (emails printed to terminal)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=dev@capaciti.org.za

# =============================================================================
# CAPTCHA Configuration
# =============================================================================
# CAPTCHA is disabled by default in local development
CAPTCHA_ENABLED=False

# If you want to test CAPTCHA locally, uncomment and add your test keys:
# CAPTCHA_ENABLED=True
# CAPTCHA_PROVIDER=recaptcha
# RECAPTCHA_PUBLIC_KEY=your-test-recaptcha-site-key
# RECAPTCHA_PRIVATE_KEY=your-test-recaptcha-secret-key

# =============================================================================
# Optional: Test Supabase Integration Locally
# =============================================================================
# Uncomment these to test Supabase storage backend on your local machine
# DEFAULT_FILE_STORAGE=pss_backend.storage_backends.SupabaseMediaStorage
# SUPABASE_URL=https://[PROJECT-REF].supabase.co
# SUPABASE_KEY=your-supabase-anon-key
# SUPABASE_STORAGE_BUCKET=pss-testing-media

# =============================================================================
# Notes:
# - This environment uses SQLite and local file storage by default
# - Emails are printed to console, not sent
# - CAPTCHA is disabled for easier testing
# - Security features (SSL, HSTS) are disabled for local HTTP
# - Generate a unique SECRET_KEY for your machine (don't share it!)
# =============================================================================
